// This file has been automatically generated for unit testing via the Karma test runner
import { TestBed } from '@angular/core/testing';

import { UpdatePatientGuard } from './update-patient.guard';

describe('UpdatePatientGuard', () => {
  let guard: UpdatePatientGuard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(UpdatePatientGuard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
